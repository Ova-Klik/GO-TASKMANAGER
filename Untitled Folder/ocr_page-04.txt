Milestone

Foundations
1 Gin basics « Routes « in-memory CRUD « JSON responses

GOAL

By the end of this Milestone you will have a working Gin web server that handles HTTP.
requests, returns JSON, and performs Create/Read operations on tasks stored in memory
(no database yet).

1.1 Installing Go & Gin

Install Go 1.21+

Download from https://go.dev/dl/ — choose the installer for your OS. After installation, verify:

go version

# Expected: go version go1.21.x...

Create the Project

Terminal
mkdir TaskManager && cd TaskManager

go mod init TaskManager

Install Gin
Terminal
go get github.com/gin-gonic/gin

TIP

Run go mod tidy after adding any new package. It cleans up go.sum and removes unused
imports.

1.2 Understanding Gin — Core Concepts

Before writing code, here are the four concepts you will use every Milestone:

Concept What It Means

gin.Engine The heart of your application. Holds all your routes and middleware.
Created with gin.Default() which includes a logger and crash recovery.

Route Handler A Go function with signature func(c *gin.Context). Gin calls this function
when a matching HTTP request arrives.

gin.Context (c) A struct passed to every handler. Use it to read the request (c.Param,
¢.ShouldBindJSON) and write the response (c.JSON, c.String).

Route Groups Lets you share a URL prefix across many routes. r.Group("/api/v1")
means every route inside uses that prefix automatically.

TaskManager — Building a REST API with Gin + Page 4 of 23
